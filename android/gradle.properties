# Project-wide Gradle settings

# JVM memory for Gradle daemon (increase if needed)
org.gradle.jvmargs=-Xmx1536m

# Enable AndroidX migration
android.useAndroidX=true

# Enable Jetifier to migrate old support library references to AndroidX
android.enableJetifier=true

# Optional: enable parallel builds for faster compilation (uncomment if needed)
# org.gradle.parallel=true
